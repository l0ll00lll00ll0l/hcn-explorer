version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: hcn-postgres
    environment:
      POSTGRES_DB: hcn
      POSTGRES_USER: hcn
      POSTGRES_PASSWORD: hcn
    ports:
      - "5433:5432"
    volumes:
      - hcn-data:/var/lib/postgresql/data

volumes:
  hcn-data:
